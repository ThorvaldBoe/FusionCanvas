## ADDED Requirements

### Requirement: View Presents Idea, Phrase, And Graphic Direction Together
FusionCanvas SHALL ensure that the view presents idea, phrase, and graphic direction together.

#### Scenario: Requirement is met
- **WHEN** the relevant workspace behavior is used
- **THEN** The view presents idea, phrase, and graphic direction together

### Requirement: Users Can Edit Or Refine Each Triangle Element
FusionCanvas SHALL allow users to edit or refine each triangle element.

#### Scenario: Requirement is met
- **WHEN** the relevant workspace behavior is used
- **THEN** the user can edit or refine each triangle element

### Requirement: Idea Element Is Required For Every Design
FusionCanvas SHALL ensure that the idea element is required for every design.

#### Scenario: Requirement is met
- **WHEN** the relevant workspace behavior is used
- **THEN** The idea element is required for every design

### Requirement: Phrase Element Can Be Marked As Not Used For Graphic-only Designs
FusionCanvas SHALL ensure that the phrase element can be marked as not used for graphic-only designs.

#### Scenario: Requirement is met
- **WHEN** the relevant workspace behavior is used
- **THEN** The phrase element can be marked as not used for graphic-only designs

### Requirement: Graphic Element Can Be Marked As Not Used For Text-only Designs
FusionCanvas SHALL ensure that the graphic element can be marked as not used for text-only designs.

#### Scenario: Requirement is met
- **WHEN** the relevant workspace behavior is used
- **THEN** The graphic element can be marked as not used for text-only designs

### Requirement: Each Triangle Element Can Be Selected As The Target For Refinement
FusionCanvas SHALL ensure that each triangle element can be selected as the target for refinement.

#### Scenario: Requirement is met
- **WHEN** the relevant workspace behavior is used
- **THEN** Each triangle element can be selected as the target for refinement

### Requirement: View Should Make Gaps Or Weak Alignment Easier To Notice
FusionCanvas SHALL ensure that the view should make gaps or weak alignment easier to notice.

#### Scenario: Requirement is met
- **WHEN** the relevant workspace behavior is used
- **THEN** The view should make gaps or weak alignment easier to notice

### Requirement: Notes Can Capture Concerns, Alternatives, Or Improvement Ideas
FusionCanvas SHALL ensure that notes can capture concerns, alternatives, or improvement ideas.

#### Scenario: Requirement is met
- **WHEN** the relevant workspace behavior is used
- **THEN** Notes can capture concerns, alternatives, or improvement ideas

### Requirement: View Should Support Human Judgment Without Requiring AI
FusionCanvas SHALL ensure that the view should support human judgment without requiring AI.

#### Scenario: Requirement is met
- **WHEN** the relevant workspace behavior is used
- **THEN** The view should support human judgment without requiring AI

### Requirement: When AI Scoring Is Available, The Middle Of The Triangle Can Show A Quality Indicator, Weak Element, Or Readiness State
FusionCanvas SHALL ensure that when AI scoring is available, the middle of the triangle can show a quality indicator, weak element, or readiness state.

#### Scenario: Requirement is met
- **WHEN** the relevant workspace behavior is used
- **THEN** When AI scoring is available, the middle of the triangle can show a quality indicator, weak element, or readiness state

### Requirement: Score Should Support Judgment Rather Than Enforce Decisions
FusionCanvas SHALL ensure that the score should support judgment rather than enforce decisions.

#### Scenario: Requirement is met
- **WHEN** the relevant workspace behavior is used
- **THEN** The score should support judgment rather than enforce decisions

### Requirement: Acceptance Criteria Remain Satisfied
FusionCanvas SHALL satisfy the acceptance criteria captured in the source PRD.

#### Scenario: User Can Review Idea, Phrase, And Graphic Direction In One Place
- **WHEN** the corresponding capability is delivered
- **THEN** A user can review idea, phrase, and graphic direction in one place.

#### Scenario: User Can Update Weak Elements
- **WHEN** the corresponding capability is delivered
- **THEN** A user can update weak elements.

#### Scenario: User Can Indicate That Phrase Or Graphic Is Intentionally Absent
- **WHEN** the corresponding capability is delivered
- **THEN** A user can indicate that phrase or graphic is intentionally absent.

#### Scenario: User Can Record Design Triangle Notes
- **WHEN** the corresponding capability is delivered
- **THEN** A user can record design triangle notes.

#### Scenario: View Helps Evaluate Creative Fit Before Production Work Continues
- **WHEN** the corresponding capability is delivered
- **THEN** The view helps evaluate creative fit before production work continues.

#### Scenario: Optional Scoring Can Identify Weak Triangle Elements Without Replacing User Judgment
- **WHEN** the corresponding capability is delivered
- **THEN** Optional scoring can identify weak triangle elements without replacing user judgment.

## Source PRD

# FC-0208 - Design Triangle View

## Summary

The Design Triangle View helps users evaluate alignment between idea, phrase, and graphic.

In the default workflow, this view is the central interaction model of the Basic Concept Tool.

## User Need

Creators need to see whether a listing's core idea, verbal hook, and visual direction support each other.

## Requirements

- The view presents idea, phrase, and graphic direction together.
- Users can edit or refine each triangle element.
- The idea element is required for every design.
- The phrase element can be marked as not used for graphic-only designs.
- The graphic element can be marked as not used for text-only designs.
- Each triangle element can be selected as the target for refinement.
- The view should make gaps or weak alignment easier to notice.
- Notes can capture concerns, alternatives, or improvement ideas.
- The view should support human judgment without requiring AI.
- When AI scoring is available, the middle of the triangle can show a quality indicator, weak element, or readiness state.
- The score should support judgment rather than enforce decisions.

## Acceptance Criteria

- A user can review idea, phrase, and graphic direction in one place.
- A user can update weak elements.
- A user can indicate that phrase or graphic is intentionally absent.
- A user can record design triangle notes.
- The view helps evaluate creative fit before production work continues.
- Optional scoring can identify weak triangle elements without replacing user judgment.

## Out of Scope

- Mandatory AI critique
- Mandatory automated scoring
- Image analysis
- Aesthetic validation

## Related Notes

- [[Roadmap]]
- [[Product]]
- [[Principles]]
- [[FC-0210 - Basic Concept Tool]]
- [[FC-0409 - Critique and Scoring]]
