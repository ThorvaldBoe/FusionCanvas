# Show application version on splash screen

Add the user-facing product version to the splash screen so startup imagery is identifiable and support screenshots retain build context. The existing version provider remains the source of truth; no versioning or splash timing changes are included.
