## 1. Presentation State and Action Inputs

- [x] 1.1 Add local editable concept-idea, phrase, and graphic-direction values to the refinement session and synchronize them safely from inspector changes.
- [x] 1.2 Capture the complete local working triangle when Fine tune or Change is invoked while preserving existing success, failure, cancellation, scoring, history, and persistence behavior.

## 2. Refinement Editor UI

- [x] 2.1 Replace the three refinement value labels with readable, wrapping text editors bound two-way to the local working values.
- [x] 2.2 Preserve stage-based read-only behavior and expose distinct accessible names and appropriate multiline behavior for each editor.

## 3. Automated Verification

- [x] 3.1 Add focused view-model tests for initialization, synchronization, current-value action payloads, local-edit side effects, failure preservation, and successful synchronization.
- [x] 3.2 Add Avalonia headless view tests for complete displayed text, two-way editing, wrapping, multiline sizing, accessible names, and read-only state.

## 4. Completion Gates

- [x] 4.1 Record criterion-level evidence for every acceptance scenario in verification.md.
- [x] 4.2 Run focused tests, the solution build and test baseline, strict OpenSpec validation, and a changed-scope regression review; resolve any in-scope failures.
