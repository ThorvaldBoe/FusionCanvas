namespace FusionCanvas.Application.Stores.Printify;

public enum PrintifyConfigurationKind
{
    Missing, Available, Saved, Verified, Unavailable, InvalidKey, PermissionDenied,
    RateLimited, NetworkFailure, UnexpectedResponse, InvalidContext
}
